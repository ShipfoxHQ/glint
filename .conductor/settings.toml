"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
archive = "docker compose down"
run_mode = "concurrent"

[scripts.run.dev]
command = "mise run local:start"
default = true
icon = "play"

[scripts.run.test]
command = "mise run local:test"
icon = "test-tube"
